
Export DISPLAY from VNC
export DISPLAY=$VNC_DISPLAY
export XDG_SESSION_TYPE=x11
export DESKTOP_SESSION=xfce

 # Start dbus if not already running
if [ -z "$DBUS_SESSION_BUS_ADDRESS" ]; then
  eval $(dbus-launch --sh-syntax)
  fi

  # Launch XFCE
exec startxfce4
